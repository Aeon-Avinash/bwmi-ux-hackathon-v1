# Design Notes

- `shell-boundaries-fixtures/index.html` -> removed Google Fonts preconnect and stylesheet imports -> keep the prototype fully self-hosted and renderable without external network access.
- `shell-boundaries-fixtures/assets/app.js` -> hid the demo acknowledgement-number hint on the tracker -> preserve the no-hints/no-guessing no-record journey.
- `shell-boundaries-fixtures/assets/app.js` -> omitted the zero-value returned card and moved the existing ₹500 leak sentence into its own row -> match the three-state money map and never visually round away the leak.
- `shell-boundaries-fixtures/assets/app.js` -> separated confirmed provenance markup from proposed provenance markup -> keep confirmed facts solid green and proposed/unconfirmed facts ochre-dashed.
- `shell-boundaries-fixtures/assets/shell.css` -> forced nav bilingual labels and hover states to light-on-blue, with a white active tab and visible keyboard focus -> fix the dark-on-dark navigation defect and meet AA contrast.
- `shell-boundaries-fixtures/assets/shell.css` -> added restrained borders, shadows, and consistent card depth -> restore the reference system's editorial interior inside government chrome.
- `shell-boundaries-fixtures/assets/shell.css` -> applied green held, dashed-neutral requested, amber unlocated, ochre-dashed proposed, and hatched leak treatments -> make status and provenance readable before the labels are parsed.
- `shell-boundaries-fixtures/assets/shell.css` -> added 390px containment for masthead, nav, forms, OTP, event rows, footer, and long clock chips -> remove journey-level horizontal overflow without shrinking text.
- `shell-boundaries-fixtures/assets/shell.css` -> kept RTL regional lines right-directed while isolating amounts, codes, clocks, and numerals LTR -> support Urdu/Kashmiri/Sindhi without reversing numeric facts.
- `shell-boundaries-fixtures/assets/shell.css` -> stopped forcing regional leak/footer text through the monospace stack -> prevent missing-script glyph boxes while retaining tabular numerals.
- `shell-boundaries-fixtures/data/ledger.json` -> replaced Asha's aggregate debit event with the authorized 21:12 ₹31,000, 21:14 ₹42,500, and 21:19 ₹25,000 Saral Bank UPI events -> make the event ledger match the locked transaction sequence without changing the ₹98,500 case total.
- `shell-boundaries-fixtures/assets/verify-fixtures.mjs` -> asserted exactly three Saral debit events, their timestamps and text amounts, and their sum against `totalPaise` -> close the verifier gap that allowed the aggregate event to pass.
- `shell-boundaries-fixtures/assets/i18n.js` -> made English primary on a fresh session and omitted a secondary line when it duplicates the primary -> match the demo default and remove redundant bilingual pairs without changing pack strings.
- `shell-boundaries-fixtures/assets/app.js` -> split clock content into a machine-label badge and muted explanatory tail -> restore the design system's distinction between clock codes and prose.
- `shell-boundaries-fixtures/assets/app.js` -> grouped complaint fields with semantic fieldsets and existing-string mono legends -> improve long-form scan order without adding or changing copy.
- `shell-boundaries-fixtures/assets/app.js` -> positioned the mock SMS from the rendered nav boundary and prevented OTP focus scrolling -> keep the 390px toast below navigation and clear of the acknowledgement input.
- `shell-boundaries-fixtures/assets/app.js` -> added a larger status-page heading class -> strengthen the status screen's editorial hierarchy.
- `shell-boundaries-fixtures/assets/shell.css` -> de-emphasized secondary pair lines, set 48px/28px section rhythm, and constrained prose to about 70 characters -> improve bilingual hierarchy and reading measure.
- `shell-boundaries-fixtures/assets/shell.css` -> pinned home-card actions, converted updates to a left-rule list, styled field groups and date/time controls, and separated clock badges from tails -> apply the requested component polish while preserving existing behavior.
- `shell-boundaries-fixtures/assets/shell.css` -> returned flag timestamps to the multilingual sans stack while retaining tabular numerals -> prevent missing-script glyphs in localized `since` labels.
- `post-fix-screenshots/*.png` -> regenerated the five required captures after the authorized data and polish pass -> keep visual evidence aligned with the final branch.
- `shell-boundaries-fixtures/assets/app.js` -> promoted every route heading to one `page-title`, normalized dateline order, and moved filed-confirmation headings outside receipt cards -> enforce the same breadcrumb/banner/title stack on home, forms, tracking, and confirmations.
- `shell-boundaries-fixtures/assets/app.js` -> rebuilt the acknowledgement interstitial as title, mono notice kicker, paired portal prose, filing track, and one action row -> make the required honesty notice the clearest block without changing its words.
- `shell-boundaries-fixtures/assets/app.js` -> routed all checklist content through the bilingual pair renderer and removed legacy inline font-size overrides -> restore all 23 locale pairings and let global type tokens govern helper text.
- `shell-boundaries-fixtures/assets/shell.css` -> normalized body, bilingual pairs, page titles, section heads, content widths, card padding, prose measure, and section rhythm -> make type and spacing consistent across every route.
- `shell-boundaries-fixtures/assets/shell.css` -> limited visible focus rings to keyboard focus, removed ring transitions, and added explicit multilingual fallback on regional text inside mono kickers -> stop mouse focus flashes while preserving keyboard visibility and native-script glyphs.
- `shell-boundaries-fixtures/assets/spine.css` -> changed the shared form-control ring from `:focus` to `:focus-visible` with no transition -> align the component source with the shell's input-modality behavior.
- `post-fix-screenshots/dispatch-f/*.png` -> captured fresh mouse-driven home, acknowledgement, and 390px tracker screens -> record the final type-system and focus-state result.
- `shell-boundaries-fixtures/assets/app.js` -> replaced the hidden static tracker hint with English-only guided-review credentials, copy/select fallback, fill-on-tap seeded cases, and mandatory mock-data fine print -> make the simulated case spine discoverable without changing platform strings or the authentic no-record path.
- `shell-boundaries-fixtures/assets/shell.css` -> styled guided-review credentials and seeded cases as compact LTR ochre-dashed demo elements, and widened desktop document/form rails to 880px/820px -> preserve boundary honesty and improve reviewer scanning without changing case/status layout or type tokens.
- `shell-boundaries-fixtures/assets/verify-fixtures.mjs` -> asserted the four guided-review acknowledgements and registered mobiles against the trackable fixture records -> prevent hint credentials from drifting from mock data.
- `post-fix-screenshots/dispatch-g/*.png` -> captured home discoverability, tracker hints in English and Urdu, seeded Asha status, and a widened acknowledgement screen -> retain visual evidence for the guided-review path.
